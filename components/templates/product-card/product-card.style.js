import styled from '@emotion/styled';

const ProductCardStyle = styled.div(() => ({
   '#discount': {
      clipPath: 'polygon(0 0, 100% 0, 100% 100%, 50% 70%, 0 100%, 0% 50%)',
   },
}));
export default ProductCardStyle;
